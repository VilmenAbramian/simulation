
# PhD RFID Simulator  
## 📖 Содержание  
- [📌 Описание](#📌-описание)  
- [📁 Структура проекта](#📁-структура-проекта)  
- [🛠 Установка](#🛠-установка)  
- [🚀 Использование](#🚀-использование)  
    - [🔍 Список доступных моделей](#🔍-список-доступных-моделей)  
    - [▶️ Запуск модели](#▶️-запуск-модели)  
- [🏗 Добавление новой модели](#🏗-добавление-новой-модели)  
    - [❗️ Обязательные правила](#❗️-обязательные-правила)  
    - [🪛 Рекомендации](#🪛-рекомендации)  
- [📌 Модели](#📌-модели)  
    - [🏓 PingPong](#🏓-pingpong)  
    - [📡 RFID](#📡-rfid)  
    - [⏱️ TagTime](#⏱️-tagtime)  
 - [📜 Лицензия](#📜-лицензия)  
 - [💁🏽 Автор](#💁🏽-автор)  
          
  
## 📌 Описание  
  
Проект посвящён дискретно-событийному имитационному моделированию протокола беспроводной связи RFID (Radio Frequency IDentification). Основной целью проекта является определение времени и вероятности успешного обмена данными между устройствами, принимающими участие в обмене данными - меткой и считывателем. Подробнее о протоколе RFID можно узнать из [Википедии](https://ru.wikipedia.org/wiki/RFID)  
  
Проект состоит из нескольких моделей, которые соответствуют определённым правилам оформления, а для их запуска можно использовать как консольный интерфейс (написан с помощью библиотеки [click](https://click.palletsprojects.com/en/stable/)), так и jupyter блокноты.  
  
Каждая модель и каждый пакет в данном проекте содержит собственный `README`, в котором подробнее описана соответствующее описание, структура, использование и т.д.  
  
## 📁 Структура проекта  
  
Пакет `pysim` имеет следующую структуру:  
  
```  
|-- pysim/  
|   |-- __init__.py  
|   |-- experiments/               # Jupyter блокноты  
|   |-- models/                    # Дискретно-событийные модели  
|   |   |-- __init__.py  
|   |   |-- pingpong/              # Модель ping-pong'a  
|   |   |-- rfid/                  # Модель RFID для движущихся объектов  
|   |   |-- tag_time/              # Аналитические и имитационные модели для оценки времени обмена данными  
|   |-- sim/                       # Пакет с ядром, который используют все модели  
|   |   |-- __init__.py  
|   |   |-- logger.py              # Кастомный логгер  
|   |   |-- simulator.py           # Ядро для имитационных моделей  
|   |-- main.py                    # Глобальный click интерфейс  
|-- tests/                         # Все тесты проекта  
|   |-- model_tests/               # Тесты моделей  
|   |   |-- test_mc2.py  
|   |   |-- test_pingpong_oop.py  
|   |-- simulator/                 # Тесты ядра  
|   |   |-- __init__.py  
|   |   |-- _paths.py  
|   |   |-- test_echo1.py  
|   |   |-- test_queue.py  
|-- .gitignore  
|-- README.md  
|-- setup.py                       # Информация и установка пакета симуляции  
```  
Подробнее о файловой структуре моделей и других пакетов внутри проекта описано в их собственных `README`.  
  
## 🛠 Установка  
  
Проект `rfidsim` распространяется как Python-пакет, что позволяет удобно устанавливать его в режиме разработки (editable mode) и использовать CLI-команду `sim` в любой точке системы. Это достигается с помощью файла `setup.py`, который содержит информацию о структуре проекта, зависимостях и точке входа.  
  
### 🔐 Клонирование репозитория  
  
Вы можете клонировать проект двумя способами:  
  
**1. Через SSH (только для владельца репозитория или если добавлен SSH-ключ):**  
```bash  
git clone git@github.com:VilmenAbramian/simulation.git
```  
  
**2. Через HTTPS (универсальный способ):**  
```bash  
git clone https://github.com/VilmenAbramian/simulation.git
```  
  
### 📦 Установка пакета  
  
1. Перейдите в корневую директорию проекта:  
```bash  
cd simulation
```  
  
2. Создайте виртуальное окружение:  
```bash  
python -m venv venv
```
3. Активируйте виртуальное окружение:
```bash
source venv/bin/activate # Для Linux/macOS
venv\Scripts\activate    # Для Windows
```
  
4. Установите пакет в режиме разработки (editable mode):  
```bash  
pip install -e .
```  
  
Если вы впервые запускаете проект на новой машине, убедитесь, что все зависимости корректно установлены. При необходимости проверьте файл `setup.py`, в котором перечислены используемые библиотеки.  
  
## 🚀 Использование  
  
После установки, находясь в корневой директории проекта, можно использовать команду `sim` для работы с моделями. Вывод программы, если вызвать `sim` без параметров  
```bash  
Usage: sim [OPTIONS] COMMAND [ARGS]...  
Options:  
 --help Show this message and exit.  
Commands:  
 list Выводит список моделей.  run Запустить модель.
 ```  
### 🔍 Список доступных моделей  
```bash  
sim list
```  
Пример вывода:  
```bash  
* pingpong
* rfid
* tag_time
```  
### ▶️ Запуск модели  
Находясь в корневой директории проекта можно использовать click-интерфейсы всех моделей с помощью команды `sim run <model_name>`. Имеется возможность как запустить выполнение модели с параметрами по умолчанию, так и задать их самостоятельно. Дополнительную справку можно вызвать с помощью `sim run <model_name> --help` либо найти в `README` соответствующей модели.  
  
Пример:  
```bash  
sim run pingpong
```  
Будет получен ответ:  
```bash  
Running PingPong modelРезультаты одиночного моделирования:Параметры: {'loss_prob': 0.8, 'interval': 10.0, 'channel_delay': 2.0, 'service_delay': 1.0, 'max_pings': 1000000}  
- Средний интервал отправки Ping клиентом(условные единицы) = 10.0  
- Средняя длительность передачи сообщения(условные единицы) = 2.0  
- Вероятность потери сообщения = 0.19953  
```  
  
## 🏗 Добавление новой модели  
### ❗️ Обязательные правила  
  
Каждая модель в проекте `rfidsim` должна соответствовать определённым правилам. Чтобы добавить новую модель нужно:  
  
1. Создать новую папку в `pysim/models`, например `new_model`.  
      
2. Внутри `new_model` создать файл `cli.py`.  
      
3. В `cli.py` добавить функцию `cli_run`, декорированную как `@click.command()`.  
      
4. Теперь при запуске `sim list` новая модель должна появиться в списке.  
      
  
Пример `cli.py`:  
```  
import click  
  
@click.command()  
def cli_run():  
 print("Running new_model simulation")
```  
### 🪛 Рекомендации  
Также необязательно, но желательно придерживаться следующих рекомендаций:  
1. Описывать логику создаваемой модели в модуле `model.py`;  
2. Описывать кастомные структуры данных для хранения информации с помощью библиотеки `pydantic` в модуле `objects.py`;  
3. Описывать функции обработчики (в особенности `initialize` и `finalize`) в модуле `handlers.py`;  
4. Обрабатывать полученные результаты в случае запуска программы из консольного интерфейса в модуле `processing.py`;  
5. Для хранения результатов после их обработки в `processing.py` следует создать директорию `results` внутри пакета модели. Текстовые данные можно хранить в формате `json` строк  
6. В случае запуска моделей с помощью Jupyter блокнотов информация выводится в этом же блокноте. При этом вспомогательные функции для обработки данных, оформления графиков и т.п. рекомендуется всё же выносить в отдельные модули;  
  
## 📌 Модели  
  
### 🏓 PingPong  
Имя пакета: `pingpong`  
  
Имитационная модель взаимодействия клиента и сервера по принципу Ping-Pong. Позволяет анализировать параметры задержки, вероятности потерь и количество отправленных сообщений. Является самой простой моделью, использующей большую часть функционала проекта `pysim`. Предназначена для демонстративных целей. Помимо возможности изучить способы взаимодействия с проектом в качестве пользователя с помощью модели `pingpong` можно разобраться с внутренним устройством типичной модели в `pysim` и таким образом добавить собственную.  
  
Запуск:  
  
```  
sim run pingpong_oop  
```  
Подробнее о данной модели описано в её собственном `README`.  
  
### 📡 RFID  
Имя пакета: `rfid`  
  
Имитационная модель системы UHF RFID с мобильными считывателями и метками, полностью реализованная на Python 3. Основная модель проекта `pysim` также является часть кандидатской диссертации автора.  
  
Модель позволяет:  
- Вычислять вероятность идентификации метки;  
- Моделировать чтение EPCID и произвольных банков памяти;  
- Моделировать одно- и двухлучевое распространение сигнала между считывателем и меткой;  
  
Запуск:  
```  
sim run rfid  
```  
Подробнее о данной модели описано в её собственном `README`.  
  
### ⏱️ TagTime  
Имя пакета: `tag_time`  
  
Имитационная и аналитическая модели оценки времени чтения RFID метки в зависимости от вероятности битовой ошибки и выбранных параметров протокола RFID. В пакете `tag_time` также представлены 2 реализации симуляции - на основе логики `pingpong` и на основе специальной матрицы. **Эта модель зависит от пакета rfid, так как ей необходимы объекты команд считывателя и ответов метки для расчёта времени обмена данными.**  
  
Запуск:  
```  
sim run tag_time  
```  
Подробнее о данной модели описано в её собственном `README`.  
## 📜 Лицензия  
  
MIT License.  
  
## 💁🏽 Автор  
Вильмен Абрамян  
  
Email: vilmen.abramian@gmail.com  
  
GitHub: [VilmenAbramian](https://github.com/VilmenAbramian/simulation)  
  
Часть 2й главы и 3я глава кандидатской диссертации.