# `rfid` - Модель радиочастотной идентификации

Имитационная модель взаимодействия между RFID считывателем и метками в условиях движения, отражений и шумов. Предназначена для численного анализа производительности RFID систем в реальных сценариях, включая поддержку многолучевого канала и параметров физического уровня.

## 📚 Оглавление
* [Структура](#🗂️-структура)
* [Параметры](#⚙️-параметры)
* [Запуск модели](#запуск-модели)
* [Модули](#Модули)

## 🗂️ Структура:
```
rfid/
├── channel.py            # Расчёт канала связи между считывателем и меткой
├── cli.py                # Точка входа, консольный click-интерфейс
├── configurator.py       # Инициализация и запуск модели
├── epcstd.py             # Объекты и логика для протокола EPC Gen2
├── handlers.py           # Обработчики событий
├── objects.py            # Объекты считывателя, меток и т.д., а также state-машина
├── params.py             # Входные данные в модель
├── processing.py         # Обработка и вывод результатов
├── results/              # Директория с результатами работы модели
│   ├── logs/             # Сохранённые логи
│   ├── plots/            # Сгенерированные в processing.py изображения
│   ├── text/             # Сгенерированные в processing.py строки с результатами
└── README.md
```

## ⚙️ Параметры:

В модели все параметры находятся в модуле `params.py`. Для этого используются pydantic модели:

* `RFIDDefaults` - содержит те параметры, которые можно задать вручную из click-интерефейса (не меняя код). При этом, если эти параметры не задаются, модель автоматически использует значения по умолчанию. Список этих параметров:

    - `tari`: длительность импульса считывателя data-0 (в мкс), например: `12.5`;
    - `encoding`: тип модуляции метки (`FM0`, `M2`, `M4`, `M8`);
    - `tid_word_size`: объём пользовательских данных, считываемых с метки (в словах);
    - `power_dbm`: мощность сигнала считывателя (в дБм);
    - `num_tags`: число меток в симуляции;
    - `speed`: скорость движения считывателя (в км/ч);
    - `reader_offset`, `tag_offset`: смещение считывателя и меток от стены (в м);
    - `altitude`: высота полёта считывателя (в м);
    - `useadjust`: активировать ли QueryAdjust;
    - `delta`: параметр чувствительности QAdjust;

* `RFIDInternalParams` - содержит прочие параметры, которые не требуется менять в рамках различных экспериментов. **Не рекомендуется изменять их значения** неопытным пользователям

## ▶️ Запуск модели:
Модель можно запускать и использовать разными способами в зависимости от поставленных задач.

Находясь в директории `pysim/models/rfid`, выполните команду:
```bash
python3 cli.py run
```

## Модули:

### `objects.py`

Модуль содержит определения ключевых физических и логических компонентов модели RFID: считыватель, метка, генератор меток, антенна, среда распространения и система сбора статистики. Это файл с основными сущностями симуляции, реализующий конечные автоматы, взаимодействие объектов и обработку сигналов.

#### ✅ Основные публичные классы:
Эти классы являются главными точками взаимодействия и могут использоваться внешними модулями (CLI, блокноты, конфигуратор):

- **Model** — главный контейнер модели: содержит считыватель, метки, генераторы, канал связи и статистику.
- **Reader** — симулируемый RFID считыватель, реализующий протокол EPC Gen2 (через конечный автомат состояний).
- **Tag** — RFID метка, реагирующая на команды и меняющая состояние в соответствии с протоколом.
- **Generator** — создаёт новые метки по мере симуляции.
- **Medium** — модель канала: включает расчёт потерь, эффекта Доплера, отражений.
- **Transaction** — единица взаимодействия между считывателем и одной/несколькими метками.
- **Statistics** — сбор, хранение и анализ статистики по всем взаимодействиям.

#### ⚙️ Внутренние классы (служебные, начинаются с подчёркивания):

Используются для реализации конечных автоматов, журналов и внутренних состояний. Внешнему пользователю напрямую не требуются:

- **_ReaderState**, **_ReaderOFF**, **_ReaderQUERY**, **_ReaderACK**, и т.д. — состояния Finite State Machine считывателя.
- **_ReaderSlot**, **_ReaderRound** — управление слотами и раундами инвентаризации.
- **_PowerControlMode**, **_AlwaysPoweredOn**, **_PeriodicPowerOn** — режимы питания считывателя.
- **_TagPowerRecord**, **_TagReadRecord**, **_TagRecord** — записи для ведения статистики по меткам.
- **_TagPowerMinMap** — вспомогательный словарь мощности в транзакции.
- И т.д.

#### 📌 Особенности:
- Физика распространения сигнала импортируется из `channel.py`: усиление, потери, доплеровский сдвиг. Благодаря этому можно реализовывать разные физические модели потерь, которые можно добавлять в симуляцию без изменения других частей кода.
- Метки используют state-machine прямо внутри класса `Tag`, а `Reader` — через отдельные классы-состояния.
- Поддержка динамического создания меток, смены раундов, конфигурации параметров инвентаризации.
- Результаты работы симуляции рассчитываются и возвращаются внутри класса `Statistics`.

`objects.py` — это фундамент модели. Его компоненты не запускаются напрямую, но формируют основу каждой симуляции.